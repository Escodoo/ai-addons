Provisions an Agno **thread** bridge for CRM leads and opportunities
(`crm.lead` → `POST /bridge/odoo`).

Depends on `agno_thread_bridge_base` for the shared auth-token ICP and field
rewrite helpers.
