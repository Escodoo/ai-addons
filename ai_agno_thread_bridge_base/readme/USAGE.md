Open a contact form. The AI bridge button on the chatter (thread usage) sends
the configured partner fields to Agno `/bridge/odoo`. The agent analyzes the
record and posts an HTML summary back on the chatter.

Queries to Odoo from the agent run as the **requesting** user (HMAC identity),
not as a bot account.
